# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

A population-based discrete-time Markov (state-transition) simulation framework evaluating health and economic impacts of NCD policies in Indonesia, projecting outcomes under policy scenarios (roughly 2025–2050 in the legacy CVD pipeline, 2017–2050 in the FAIR Choices pipeline, 2025–2100 in the integrated pipeline).

**Core design**: Population → Risk Factors → Markov transitions → Outcomes (deaths, DALYs, costs). Conditional independence across diseases; PAF-based incidence; no comorbidity modeled (v1).

The codebase currently contains **four coexisting code families**. Know which one you are working in:

1. **Legacy CVD pipeline** (`code/cvd/`) — the original numbered pipeline, with a generic/SEARO variant (`*.R`) and an Indonesia-specific variant (`*_indonesia.R`). Entry point `code/cvd/00_run_model_indonesia.R`. Models six causes (IHD, ischemic stroke, intracerebral hemorrhage, hypertensive heart disease, rheumatic heart disease, cardiomyopathy & myocarditis); the generic variant models the first four.
2. **FAIR Choices CVD pipeline** (`code/cvd-fair-choices/`) — the **most actively developed** family. Started as a copy of the Indonesia CVD scripts but has grown into a full-lifecycle (ages 0–95) `00`→`10` pipeline modeling **seven causes** (IHD, ischemic stroke, intracerebral hemorrhage, hypertensive heart disease, rheumatic heart disease, cardiomyopathy & myocarditis, type-2 diabetes) plus an all-cause envelope. Entry point `code/cvd-fair-choices/00_run_model_cvd_fair.R`; default Nelder-Mead calibration and the `_fair` scenario engine. Adds two workbook-driven **intervention families** (clinical FAIR Choices + public-health: sodium, TFA, tobacco/Jha, SSB), health-outcome/DALY (07), VSL/VSLY economic value (08), costing/budget-impact/BCA cost-value Excel workbooks (09), validation (10), a financial-risk-protection quintile allocation (`10-frp`), plus isolated 70-30-30→70-70-70 treatment-cascade runners (national + 38-province subnational) and an experimental epidemiology-alignment calibration harness.
3. **Integrated NCD pipeline** (`code/scripts/` + `code/R/`) — a newer rewrite that unifies CVD, cancer, and other NCDs under a single cause-agnostic Markov engine driven by a 23-cause registry. Entry point `code/scripts/00_run_all.R`. **In active development** (V1 scope: 4 CVD causes + cervical cancer). This is the strategic direction.
4. **Cancer pipeline** (`code/cancer/`) — Markov + cohort-component population projection for 16 cancer types (GBD 2021). Run sequentially from `code/cancer/scripts/`.

## Directory Layout

```
├── code/
│   ├── cvd/               # Legacy CVD Markov pipeline (generic + _indonesia variants)
│   ├── cvd-fair-choices/  # FAIR Choices CVD pipeline (00–10 + cost/value + FRP)
│   │   ├── 70_30_30_to_70_70_70_subnational/  # stand-alone 38-province cascade (04–09)
│   │   └── alignment_tests/                   # epidemiology-alignment harness + tests
│   ├── scripts/           # Integrated NCD pipeline orchestrated by 00_run_all.R (01–07)
│   ├── R/                 # Integrated-pipeline modules: engine, cause_registry,
│   │                      #   interventions_cvd, validation, packages
│   ├── utils/             # ⚠ Identical duplicate of code/R/ (stale mirror; see Gotchas)
│   ├── cancer/            # Cancer Markov + cohort component projection
│   │   ├── fnx/           # Function library (~20 reusable functions)
│   │   └── scripts/       # Numbered execution scripts (00–06)
│   ├── demography/        # Placeholder (README only)
│   └── tests/             # Placeholder (README only)
├── data/
│   ├── raw/               # GBD 2023, UNWPP 2024, IHME, NCD-RisC, ETIHAD (not on GitHub)
│   ├── interim/           # Working/intermediate datasets
│   ├── processed/         # Production-ready .rds/.csv/.xlsx (legacy pipelines)
│   ├── gbd/               # Cleaned GBD inputs generated by integrated scripts 01/01b/03
│   ├── wpp/               # WPP2024 demographic backbone from integrated script 02
│   ├── model/             # Calibration + baseline/scenario objects (integrated 04–06)
│   ├── indonesia_model_inputs.xlsx                       # clinical FAIR analytic contract
│   ├── indonesia_model_inputs_public_health*.xlsx        # public-health analytic contract(s)
│   └── indonesia_70_30_30_to_70_70_70_inputs*.xlsx       # cascade contracts (national + subnational)
├── output/                # LEGACY + FAIR Choices pipeline outputs
│   ├── out_model/         # Per-run raw simulation .rds (gitignored; consumed by validation + 07–09)
│   ├── indonesia_model_cost_value*.xlsx                  # Model 09 cost/value workbooks
│   ├── FRP_data_structures.xlsx                          # Model 10-frp quintile allocation
│   ├── 70_30_30_to_70_70_70/            # national cascade run (isolated)
│   ├── 70_30_30_to_70_70_70_subnational/  # 38-province cascade run (isolated)
│   ├── paper/             # Publication-ready summary .rds files
│   └── slides/            # Presentation-ready .rds + .png files
├── outputs/               # INTEGRATED pipeline outputs (tables/, figures/, validation/)
├── scenarios/             # R Markdown reports: cvd/ (aim1_report.Rmd), cancer/
├── reports/               # LaTeX/Beamer decks: executive_slides.Rmd (WB–UW briefing),
│                          #   technical_presentation.Rmd/.tex, preamble, refs
├── dev/                   # ~19 design/change-log markdown docs for the FAIR Choices work
├── R/, config/, tests/    # Top-level placeholders (README only)
└── docs/                  # Documentation, references, diagrams, prompts.txt
```

`uw-wb-indonesia-ncd.Rproj` at the repo root defines the RStudio project (and `here::here()` root). A scratch directory `../temp/` (sibling of the repo) is auto-created by the legacy run scripts for intermediate processing.

## Running the Models

### Integrated NCD pipeline (active development)

```r
source("code/scripts/00_run_all.R")   # runs scripts 01–07 in order
```

`00_run_all.R` sources the numbered scripts; each can also run standalone once its prerequisites exist. Steps 01–03 are one-time data prep (re-run only when GBD/WPP data changes); step 04 re-runs on calibration changes; steps 05–07 run every model update.

### Legacy CVD pipeline

```r
source("code/cvd/00_run_model_indonesia.R")   # active Indonesia pipeline
source("code/cvd/00_run_model.R")             # generic/SEARO (legacy)
```

The entry script hard-codes `wd` to an absolute local path — update it before running on another machine. Configuration flags near the top control which stages execute:
- `run_calibration_par` — parallel calibration
- `run_adjustment_model` — post-calibration adjustments (set `FALSE` when using transparent calibration, see Gotchas)
- `run_adjustments_inputs` — baseline rate adjustments
- `run_bgmx_trend` — background mortality secular trends
- `run_CF_trend` / `run_CF_trend_80` — case fatality secular trends (80% = net of HTN control contribution)
- `run_CF_trend_ihme` — IHME-based case fatality trend (default `FALSE`)
- `run_aod_par` — dementia model (not yet implemented, default `FALSE`)

Set flags to `FALSE` to skip expensive stages when intermediate `.rds` files already exist.

### FAIR Choices CVD pipeline

```r
source("code/cvd-fair-choices/00_run_model_cvd_fair.R")   # full 00→10 run
```

Hard-codes an absolute `wd`; shares the legacy flag/config convention (`run_calibration_par`, `run_bgmx_trend`, `run_CF_trend(_80)`, etc.) and by default sources `03_calibration_indonesia_nelder_mead.R`, `05_build_baseline_indonesia.R`, then `06_run_scenarios_indonesia_fair.R` → `07`/`08`/`09`/`10`. Additional switches at the top of `00`:
- `run_clinical_interventions` / `run_public_health_interventions` — which intervention **families** Models 04/06/09 analyse (both `FALSE` → hard error; baseline always runs).
- `run_cost_value` — run Model 09 (costing / budget impact / cost-effectiveness).
- `strict_model_input_validation` — `TRUE` STOPs on any FAIL-level workbook issue; `FALSE` drops the invalid links/scenarios and continues.
- `tobacco_timing_analysis` — `"base"` (Jha piecewise scalars) vs `"normalized_exponential_lag"` (sensitivity).
- **Ages and modeled causes are the single source of truth in `00`** (`cause_map`, `min_model_age`/`max_model_age = 0/95`, `cvd_40q30_cause_codes`); an early `local({})` block fails fast on any inconsistency. All *analytic* assumptions (effects, coverage, costs, discounting, years) live in the Excel input workbooks — never in `00`. The SSB→T2DM mortality link is toggled only by the workbook `include_flag` (ships disabled).

Isolated variant runners (each starts a fresh session — run standalone, not via `00`):
```r
source("code/cvd-fair-choices/00_run_70_30_30_to_70_70_70.R")                      # national cascade
source("code/cvd-fair-choices/70_30_30_to_70_70_70_subnational/00_run_70_30_30_to_70_70_70_subnational.R")  # 38-province cascade
source("code/cvd-fair-choices/10-frp_indonesia.r")                                # FRP quintile allocation (post-run)
```
The alignment harness is run from the shell (or `source()`d): `Rscript code/cvd-fair-choices/alignment_tests/run_alignment_pipeline.R`.

### Cancer pipeline

Cancer scripts run sequentially from `code/cancer/scripts/` (00–06). Initialize with `library.R`, which loads packages, reads `settings.yml`, and sources all functions from `fnx/`. Requires internet access for remote helper functions (falls back to local paths via `settings.yml`). Note: `settings.yml` is not committed and must be present locally; `library.R` resolves paths relative to a `root` ("R/") prefix it expects to be set.

## Package Dependencies

No `renv.lock` exists. Install manually:

```r
# Legacy CVD + FAIR Choices pipeline (FAIR Choices additionally needs openxlsx
# for the Model 09 cost/value + FRP workbooks)
install.packages(c("data.table", "dplyr", "tidyr", "ggplot2", "RColorBrewer",
                   "readxl", "openxlsx", "countrycode", "stringr", "parallel",
                   "doParallel", "foreach", "gmodels", "forecast"))

# Integrated NCD pipeline (requires R >= 4.1.0 for the native pipe |>)
install.packages(c("here", "dplyr", "tidyr", "readr", "purrr", "tibble",
                   "ggplot2", "scales", "abind", "stringr", "readxl",
                   "cowplot", "data.table", "countrycode", "rlang"))
devtools::install_github("PPgp/wpp2024")   # demographic backbone (integrated script 02)

# Cancer (additional)
install.packages(c("tidyverse", "yaml", "curl"))
```

`code/R/packages.R` checks for the integrated pipeline's dependencies and stops with an install instruction if any are missing (it does not install silently).

## Integrated NCD Pipeline Architecture

`code/scripts/` holds the numbered pipeline; `code/R/` holds the shared engine and metadata sourced by those scripts (via `here("R", ...)` — see Gotchas). The cause-agnostic design means adding a disease = writing a new cause spec, with zero new engine code.

### Shared modules (`code/R/`)

| Module | Role |
|--------|------|
| `packages.R` | Dependency check; stops with install instructions if packages are missing |
| `engine.R` | Markov engine (~1,900 lines): demographic backbone (`project_population_exposure`), core runner (`run_cause_module`, `run_direct_mortality_module`), TPM validation, output helpers (`compute_q4030`, `compute_deaths_averted`, `make_module_target_mx`), and scenario/module factories. Separates the WPP **demographic backbone** from the **disease state layer**; TPM rows = FROM, columns = TO, rows sum to 1 |
| `cause_registry.R` | `CAUSE_REGISTRY` of 23 cause specs (4 CVD, 16 cancer, 3 other NCD) plus `SHARED_PARAMS`, `CVD_CAUSE_MAP`, `CVD_MODEL_TYPE` (`tpm` vs `direct_mortality`), and registry helpers. Metadata only — state vectors, transition adjacency, calibration targets, intervention slots |
| `interventions_cvd.R` | V1 simplified CVD intervention modifiers returning `eff_ir` / `eff_cf` age vectors (antihypertensives, statins, sodium, TFA, diabetes-specific BP). See header for the simplifications vs the parent CVD model |
| `validation.R` | `run_core_invariant_suite()` — cross-layer STOP/WARN/INFO invariant checks (backbone alignment, population plausibility, mx non-negativity, cause fractions ≤ 1) |

### Numbered scripts (`code/scripts/`)

| Script | Role |
|--------|------|
| `00_run_all.R` | Master orchestrator; sources 01–07 |
| `01_prepare_gbd_inputs.R` | One-time: raw GBD downloads → clean disease + PAF tables in `data/gbd/` |
| `01b_prepare_sbp_rr.R` | Build GBD 2021-style SBP dose-response RR lookup (`data/gbd/gbd_rr_sbp*.rds`) |
| `02_build_demography.R` | wpp2024 → `sf.wpp` demographic backbone (`data/wpp/`); custom `get.par()`/`get.lt()` defined in-script (from the NCD Countdown parent model) |
| `03_build_cause_fractions.R` | Logit-space trend fit through 7 GBD anchor years (2000–2023), projected annually; core identity `mx_cause(t) = mx_WPP(t) × frac_cause(t)` |
| `04_calibrate_modules.R` | Fit CVD TPMs (IHD, ischemic stroke, ICH), store HHD direct-mortality rates, fit 7-state cervical cancer TPM → `data/model/calibration/` |
| `05_run_baseline.R` | Project baseline 2025–2100 (4 CVD causes + cervical); year-varying TPMs anchored to WPP × projected cause fraction |
| `06_run_scenarios.R` | Project 12 scenarios (10 CVD + 2 cervical): baseline, bp_fast/slow, statins_fast/slow, sodium, tfa, diabetes_bp, all_fast/slow, cancer_fast/slow |
| `07_make_outputs.R` | Compile summary tables + figures (deaths averted, 40q30 trajectories, SDG 3.4 pace) to `outputs/` |

## Legacy CVD Pipeline Architecture

The entry script sources orchestration scripts that in turn source numbered sub-scripts. Most stages have a generic version and an `_indonesia` version. The active `00_run_model_indonesia.R` sources the `_indonesia` variants of stages 02, 05, 06 and adds stage 09. The FAIR Choices pipeline (`code/cvd-fair-choices/`) began as a copy of the Indonesia `_indonesia` scripts but has since diverged — see its own architecture section below.

| Script (generic / `_indonesia`) | Role |
|--------|------|
| `00_run_model.R` / `00_run_model_indonesia.R` | Entry point; sets paths, defines config flags, sources all stages |
| `01_utils(_indonesia).R` | Utility functions: `get.bp.prob()`, TFA mortality reduction, ETIHAD RR calculations |
| `02_load_inputs(_indonesia).R` | Orchestrator — sources 021–023 below |
| → `021_get_base_rates(_indonesia).R` | Extracts GBD 2023 mortality + prevalence; interpolates to single-year ages; emits `baseline_rates_part*.rds`, `locs.rds` |
| → `022_get_tps(_indonesia).R` | Health-state transition probabilities; emits `tps_inpt_part*.rds` |
| → `023_get_tps_bgmx(_indonesia).R` | Background mortality trend forecasts |
| `03_calibration.R` | Orchestrator — sources generic 031 + 032 (two-pass grid-search calibration) |
| → `031_calibration(_indonesia).R` | Calibrates to GBD estimates (±5% IR/CF grid; can run in parallel) |
| → `032_adjustments(_indonesia).R` | Second asymmetric grid; enforces IR + BG.mx ≤ 1; emits `adjustments2023_age.csv` |
| `03_calibration_indonesia_transparent.R` | **Alternative** single-pass calibration replacing 031+032 with one documented symmetric multiplicative search; writes `adjusted_searo_part*.rds` |
| `03_calibration_indonesia_nelder_mead.R` | **Alternative** calibration: Nelder-Mead downhill simplex (base R `optim`) per IR/CF multiplier vector. Used by the FAIR Choices entry point |
| `03_calibration_indonesia_random_tp.R` | **Alternative** calibration: pure random (Monte Carlo) search over multiplier vectors |
| `04_define_interventions.R` | Defines policy scenarios (sodium, TFA, statins, HTN control); emits scenario `.rds` and `covfxn2.csv` |
| `05_build_baseline(_indonesia).R` | Loads population + risk factor data; prepares Markov inputs (re-applies `adjustments2023_age.csv` when `run_adjustment_model == TRUE`) |
| `06_run_scenarios(_indonesia).R` | Core simulation engine; applies interventions via ETIHAD/GBD RRs; Indonesia variant runs in parallel, writes per-chunk `.rds` to `output/out_model/` |
| `06_run_scenarios_indonesia_fair.R` | FAIR Choices scenario engine — a legacy stub also exists here, but the actively-developed copy lives in `code/cvd-fair-choices/` and the two have **diverged** (see that pipeline's section) |
| `07_output_dalys.R` | Computes YLL, YLD, DALYs using GBD disability weights + WPP 2024 life tables |
| `08_economic_value_calculation.R` | Healthcare costs, productivity losses, cost-effectiveness (VSL/VSLY) |
| `09_validation_indonesia.R` | Validation: baseline model deaths/rates vs GBD 2023 and UNWPP 2024 by cause and sex |

**Note**: the three alternative calibration scripts (`transparent`, `nelder_mead`, `random_tp`) are near-clones differing only in the per-combo search strategy. `03_calibration.R` sources the *generic* `031`/`032` even from the Indonesia entry point; to calibrate with Indonesia-specific or alternative logic, source those scripts directly.

## FAIR Choices CVD Pipeline Architecture

`00_run_model_cvd_fair.R` sources the numbered stages 01–10 in order. Stages 01–06 mirror the legacy `_indonesia` scripts (utils → load inputs → calibration → interventions → baseline → scenarios) but drive everything off the central `cause_map`/age config in `00` and off two Excel input workbooks. Stages 07–10 are the newer analytic layer.

| Script | Role |
|--------|------|
| `01_utils_indonesia.R` | Utilities + the authoritative single-age ↔ GBD-band mapping (`gbd_age_bands()`, `gbd_band_label()`, `gbd_band_midpoint()`) |
| `02_load_inputs_indonesia.R` → `021`/`022`/`023` | Base rates, health-state TPs, background-mortality trends (as legacy, but over the 7-cause × ages 0–95 grid) |
| `03_calibration_indonesia_nelder_mead.R` | Default calibration (Nelder-Mead per IR/CF multiplier vector). Alternatives (`transparent`, `random_tp`, `alignment`) also present |
| `04_define_interventions_indonesia.R` | Data-prep (sodium/IHME salt, TFA, statins, HTN control) **plus** the workbook-driven catalogue builder for BOTH families: exports `fair_scenarios`/`fair_inputs` (clinical) and `public_health_scenarios`/`public_health_inputs` (PH: sodium, TFA, tobacco/Jha, SSB→T2DM). Row-level inclusion is decided solely by the workbook `Intervention_Cause_Map$include_flag`. Also holds the flag-guarded cascade-catalogue branch |
| `05_build_baseline_indonesia.R` | Full-lifecycle baseline: newborns enter at age 0 each year; the 95+ terminal stock is open-ended (accumulates) |
| `06_run_scenarios_indonesia_fair.R` | Scenario engine: adds the DCP/FAIR-Choices CVD package; applies coverage-adjusted multiplicative effects to IR/CF/prevalence (effect sizes from `data/raw/FAIR-Choices/taxonomy_effect_size.xlsx`); tobacco sick→dead (Jha) and SSB→T2DM mortality via the PH workbook; writes `output/out_model/model_output_*.rds` |
| `07_output_dalys.R` | **Health-outcome source of truth** for 08/09: per (loc × scenario × year × age × sex × cause) deaths/YLD/YLL/DALY + baseline-paired *averted* outcomes and CVD 40q30. Writes `dt_output_dalys.rds`, `07_life_expectancy_lookup.rds`, `07_disability_weights.rds`, `07_cvd_40q30*.rds` |
| `08_economic_value_calculation.R` | VSL/VSLY monetary valuation on the 2019 Robinson et al. Reference Case; pure consumer of Model 07. Writes `08_vsl_results.*`, summary tables, `08_bca_parameters.rds` |
| `09_cost_value.R` | Costing, budget impact, and benefit-cost analysis. Emits an R-value workbook (`indonesia_model_cost_value.xlsx`) and live-formula editions: clinical (`..._formulae.xlsx`), public-health (`indonesia_cost_value_public_health_formulae.xlsx`), and — when both families run — a combined `..._clinical_public_health_formulae.xlsx`. BCA sheets (`Health_Outcomes`/`Economic_Value`/`Benefit_Cost`) built by the shared `build_bca_sheets_into()`. Also writes `09_deck_results.rds`, the BCA-free contract the executive deck reads |
| `10_validation_indonesia.R` | Benchmarks model 2017–2050 mortality cases/rates (ages 20+, by sex/cause) against GBD 1990–2023 and UN WPP2024 |
| `10-frp_indonesia.r` | Post-processing: proportional allocation of completed **national clinical** results across wealth quintiles Q1–Q5 (re-aggregates to national by construction) → `output/FRP_data_structures.xlsx` |

**Intervention families & scenarios** (all dynamic and workbook-driven — excluded interventions never appear): clinical `fair_scenarios` = `baseline` + one per valid intervention + combined `all`; public-health `public_health_scenarios` = `baseline` + one per intervention + a joint scenario per parent package (tobacco, salt/SSB) + `all_public_health`; when both families run, a genuine joint `all_clinical_public_health` scenario is added. Analytic assumptions live only in the input workbooks; `00` carries execution switches only.

**Isolated 70-30-30→70-70-70 cascade runs.** The national runner `00_run_70_30_30_to_70_70_70.R` feeds `data/indonesia_70_30_30_to_70_70_70_inputs.xlsx` through Models 01–09 with `run_cascade_70_30_30_to_70_70_70 = TRUE` (clinical machinery only, PH off; Model 04 builds a `baseline` + `S_70_30_30_TO_70_70_70` catalogue); all output lands under `output/70_30_30_to_70_70_70/`, and a fail-fast collision guard asserts no ordinary output is touched. The subnational pipeline (`70_30_30_to_70_70_70_subnational/`) is fully **stand-alone** — adapted copies of Models 04–09 living in that directory, **never** sourcing production `00–09` and skipping stages 01/02/03; it starts from the pre-calibrated `data/processed/b_rates_full_period_reconciled_2017_2050_national_current38.rds` (Indonesia + 38 provinces) and writes only under `output/70_30_30_to_70_70_70_subnational/`.

**Alignment calibration (experimental, isolated).** `03_calibration_indonesia_alignment.R` + `05_build_baseline_indonesia_alignment.R` + `06_run_scenarios_indonesia_fair_alignment.R` recalibrate the well–sick–dead Markov to reproduce an externally-projected epidemiology (2023–2050) via **flow-inversion** (forward-solve) rather than Nelder-Mead multipliers. Run only through `alignment_tests/run_alignment_pipeline.R`; non-destructive (writes to `data/processed/alignment/` and `output_alignment/`). `06_..._alignment.R` is a thin wrapper that repoints `wd_outp` and re-runs the *unmodified* production Model 06, keeping the output contract byte-identical for Models 07–09.

**Tests** (source directly; not wired into `00`): `tests_acceptance_0006.R` (ages-0-95 + dm2 full-lifecycle refactor), `tests_fair_cost_value.R` (fast static checks of the workbook-driven cost/value extension), `tests_tobacco_ssb_mortality.R` (tobacco-Jha / SSB→T2DM mortality), and the `alignment_tests/` suite (`test_alignment_prep.R`, `validate_baseline_vs_projection.R`, `test_downstream_compat.R`).

**Executive deck.** `reports/executive_slides.Rmd` (WB–UW Beamer briefing) is BCA-free and reads only `output/09_deck_results.rds` + the workbook `Scenario_Catalog` — no Excel recalculation.

## Cancer Pipeline Architecture

Scripts in `code/cancer/scripts/` run in numbered order. Functions live in `code/cancer/fnx/` and are auto-sourced by `library.R`.

| Script | Role |
|--------|------|
| `00_read_data.R` | Read raw GBD/inputs |
| `01_process_country_param.R` | Country-level parameters |
| `02_process_bsln_scen.R` | Baseline scenario processing |
| `03_process_pop_proj_bsln_dt.R` | Baseline population projection (`*_old.R` is a superseded variant) |
| `04_process_intv_scen.R` | Intervention scenario processing |
| `05_process_pop_proj_intv_dt.R` | Intervention population projection |
| `06_dissertation_output.R` | Output/figures for write-up |

`Diagnostics.Rmd` (in `scripts/`) and `scratchpad.R` are exploratory, not part of the run order.

Key function groups:
- **Markov core**: `calc_bsln_tps_dt`, `calc_intv_tps`, `project_markov_trace_dt`, `correct_markov`
- **Population projection**: `run_ccpm` (cohort component projection model), `proj_ccpm_markov_wip` (WIP integration)
- **Metrics**: `calc_metric_dt`, `calc_rates`, `gen_lifetable`
- **Scenario processing**: `process_intv_scen_inputs`, `id_target_cc_methods`

## Legacy Intervention Scenario Logic (`code/cvd/04_define_interventions.R`)

**Sodium**: Linear interpolation 2025–2030 to target (−15% Progress, −30% Aspirational), then held flat. Affects BP distribution via `get.bp.prob()` coefficients.

**Trans-fat (TFA)**: RR = 1.28 per 2% TFA. Linear scale-down to 0%; once eliminated stays 0.

**Statins**: Linear scale-up to 60% coverage by 2050 (or logistic S-curve option).

**HTN control** (most complex, ~850 lines): Three scenarios using quadratic scale-up functions fit to NCD-RisC historical data (1990–2019). Outputs `covfxn2.csv` with columns: `location, year, control, Progress, Aspirational, Business_as_usual, p_change, a_change, aroc`.

Legacy state space per age/sex/location/cause/year: **Susceptible → Incident → Prevalent → Dead**. Intervention effects applied via ETIHAD trial RRs per 10 mmHg BP reduction, coverage-adjusted: `IR_treated = IR_baseline × (1 − coverage × RR_effect)`. Stacking order: Sodium → BP shift → IR reduction; Statins → direct IR reduction; TFA → IR via RR; BP control coverage → treatment effect.

## Quality Checks

The top-level `code/tests/` and `tests/` dirs are placeholders. The FAIR Choices pipeline is the exception — it ships runnable acceptance/QA scripts (`code/cvd-fair-choices/tests_*.R` and `alignment_tests/`, see that section); everything else keeps validation embedded:
- FAIR Choices: `00`'s early `local({})` config guards; Model 04 fails on invalid workbook links (unless `strict_model_input_validation = FALSE`, which drops them); Models 08/09 fail with a consolidated diagnostic if Model 07 outputs are missing or scenario IDs don't reconcile; Excel formula workbooks carry QA formulas reconciling Excel results against the R engine; the cascade/alignment runners have fail-fast isolation/collision guards
- Integrated: `code/R/engine.R` validates every TPM (square, [0,1], rows sum to 1); `code/R/validation.R` `run_core_invariant_suite()` enforces cross-layer invariants; `make_module_target_mx()` fails loudly on missing cause × year × sex × age targets
- `021_get_base_rates.R`: `print(anyNA(data.out))` after joins
- `032_adjustments.R`: `test = ifelse(IR + BG.mx > 1, 1, 0)` flags invalid transition probabilities
- `03_calibration_indonesia_transparent.R`: end-of-run `stopifnot` asserting IR/CF ∈ [0,1], IR/CF + BG.mx ≤ 1, and row/schema parity with input
- `09_validation_indonesia.R`: cross-source comparison of baseline mortality vs GBD 2023 and UNWPP 2024
- Cancer: `correct_markov.R` validates/corrects Markov transition matrices

## Gotchas

- **`code/utils/` duplicates `code/R/`**: the five files are byte-identical copies. The integrated scripts source `code/R/`; `code/utils/` is a stale mirror — edit `code/R/` and treat `code/utils/` as disposable.
- **Integrated-pipeline module path**: scripts source modules via `here("R", ...)`, which resolves to the **repo-root** `R/` directory — but that directory currently holds only a README; the real modules live in `code/R/`. Reconcile the path (or place/symlink the modules at repo-root `R/`) before running the integrated pipeline standalone.
- **Hard-coded paths**: legacy CVD and FAIR Choices entry scripts set `wd` to an absolute Windows path; edit before running elsewhere. The integrated pipeline uses `here::here()` instead.
- **Double-calibration**: if you use `03_calibration_indonesia_transparent.R` (or another alternative), set `run_adjustment_model <- FALSE` before stage 05, or `adjustments2023_age.csv` from 032 will be multiplied in a second time.
- **Multiple pipelines share intermediate files**: the generic, `_indonesia`, and FAIR Choices CVD scripts read/write overlapping intermediate files in `data/processed/`. Run one pipeline end-to-end rather than mixing stages.
- **FAIR Choices assumptions live in Excel, not code**: analytic assumptions (intervention–cause links, effects, coverage, costs, discounting, years, price year) belong to the input workbooks (`indonesia_model_inputs*.xlsx`); `00_run_model_cvd_fair.R` carries only execution switches. Never hard-code an assumption in the scripts, and edit ages/causes only in `00`'s `cause_map`/age config.
- **Isolated runs must stay isolated**: the cascade runners (`00_run_70_30_30_to_70_70_70.R`, the subnational `00_...`) and the alignment harness each start with `rm(list=ls())` and must be run in a fresh session — not sourced from `00_run_model_cvd_fair.R`. They write only to their own output trees (`output/70_30_30_to_70_70_70*/`, `output_alignment/`, `data/processed/alignment/`) and guard against clobbering ordinary output.
- **FAIR Choices vs legacy `06_run_scenarios_indonesia_fair.R`**: both `code/cvd/` and `code/cvd-fair-choices/` contain this file but they have diverged; the actively-maintained engine is the one in `code/cvd-fair-choices/`.
- **Raw data not on GitHub**: `data/raw/**` is gitignored (only `.md` files kept). Models expect GBD 2023, UNWPP 2024, IHME, NCD-RisC, and ETIHAD inputs staged locally; the integrated pipeline additionally generates `data/gbd/`, `data/wpp/`, and `data/model/` from its 01–04 steps.
- **Output dirs differ**: legacy pipelines write to `output/`; the integrated pipeline writes to `outputs/`. `output/out_model/` is gitignored.
